require "spec_helper"

# describe "#parsed_url" do
# it "takes in an argument of a zip code, parses that into lat/long coordinates, and outputs a url containing the lat and long." do 
#   expect(@url).to eq(["123, 456"])
# end 

end